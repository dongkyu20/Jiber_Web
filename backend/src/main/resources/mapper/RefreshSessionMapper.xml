<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiber.backend.auth.mapper.RefreshSessionMapper">
    <resultMap id="RefreshSessionRecordMap" type="com.jiber.backend.auth.mapper.RefreshSessionRecord">
        <constructor>
            <idArg column="refresh_session_id" javaType="java.lang.Long"/>
            <arg column="user_id" javaType="java.lang.Long"/>
            <arg column="refresh_token_hash" javaType="java.lang.String"/>
            <arg column="rotated_from_session_id" javaType="java.lang.Long"/>
            <arg column="user_agent" javaType="java.lang.String"/>
            <arg column="ip_address" javaType="[B"/>
            <arg column="expires_at" javaType="java.time.OffsetDateTime"/>
            <arg column="revoked_at" javaType="java.time.OffsetDateTime"/>
            <arg column="created_at" javaType="java.time.OffsetDateTime"/>
            <arg column="updated_at" javaType="java.time.OffsetDateTime"/>
        </constructor>
    </resultMap>

    <insert id="insert" parameterType="com.jiber.backend.auth.mapper.RefreshSessionInsertCommand">
        INSERT INTO refresh_sessions (
            user_id,
            refresh_token_hash,
            rotated_from_session_id,
            user_agent,
            ip_address,
            expires_at
        ) VALUES (
            #{userId},
            #{refreshTokenHash},
            #{rotatedFromSessionId},
            #{userAgent},
            #{ipAddress},
            #{expiresAt}
        )
    </insert>

    <select id="findByTokenHash" resultMap="RefreshSessionRecordMap">
        SELECT
            refresh_session_id,
            user_id,
            refresh_token_hash,
            rotated_from_session_id,
            user_agent,
            ip_address,
            expires_at,
            revoked_at,
            created_at,
            updated_at
        FROM refresh_sessions
        WHERE refresh_token_hash = #{refreshTokenHash}
    </select>

    <select id="findActiveByTokenHash" resultMap="RefreshSessionRecordMap">
        SELECT
            refresh_session_id,
            user_id,
            refresh_token_hash,
            rotated_from_session_id,
            user_agent,
            ip_address,
            expires_at,
            revoked_at,
            created_at,
            updated_at
        FROM refresh_sessions
        WHERE refresh_token_hash = #{refreshTokenHash}
          AND revoked_at IS NULL
          AND expires_at &gt; #{now}
    </select>

    <update id="revokeBySessionId">
        UPDATE refresh_sessions
        SET revoked_at = COALESCE(revoked_at, #{revokedAt}),
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE refresh_session_id = #{refreshSessionId}
    </update>

    <update id="revokeByTokenHash">
        UPDATE refresh_sessions
        SET revoked_at = COALESCE(revoked_at, #{revokedAt}),
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE refresh_token_hash = #{refreshTokenHash}
    </update>

    <update id="revokeSessionFamily">
        WITH RECURSIVE session_family AS (
            SELECT refresh_session_id
            FROM refresh_sessions
            WHERE refresh_session_id = #{refreshSessionId}
            UNION ALL
            SELECT child.refresh_session_id
            FROM refresh_sessions child
            INNER JOIN session_family parent
                ON child.rotated_from_session_id = parent.refresh_session_id
        )
        UPDATE refresh_sessions
        SET revoked_at = COALESCE(revoked_at, #{revokedAt}),
            updated_at = CURRENT_TIMESTAMP(6)
        WHERE refresh_session_id IN (
            SELECT refresh_session_id
            FROM session_family
        )
    </update>
</mapper>
